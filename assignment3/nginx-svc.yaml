apiVersion: v1
kind: Service
metadata:
  name: nginx-svc
  labels:
    run: nginx-svc
spec:
  selector:
    # app: nginx
    run: nginx-svc
  ports:
    - protocol: TCP
      port: 80
  type: ClusterIP